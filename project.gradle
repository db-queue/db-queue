System.setProperty("platformLibraryProjectVersion", "7.+")

repositories {
    maven { url 'https://nexus.yooteam.ru/repository/maven-backend-build/' }

    dependencies {
        classpath 'ru.yandex.money.gradle.plugins:yamoney-library-project-plugin:' +
                System.getProperty("platformLibraryProjectVersion")
    }
}